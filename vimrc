set nocompatible

let mapleader = ","

filetype plugin on
filetype indent on

set autoread "refresh file when changed
set autowrite "write on :make, bufferchange, etc.

set backspace=eol,start,indent

syntax enable

set number "enable line numbers

set encoding=utf8

set expandtab
set smarttab

set shiftwidth=4
set tabstop=4

set autoindent

set wrap
